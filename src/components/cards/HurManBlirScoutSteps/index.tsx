import { bliLedareCardContent } from "@/data/bliLedareCardContent"
import { BlirScoutStepsContent } from "@/data/blirScoutStepsContent"

const HurManBlirScoutSteps = () => {
  return (
    <div>
      {BlirScoutStepsContent && bliLedareCardContent.map((entry, index) => (
        <div>hello</div>
      ))}

    </div>
  )
}

export default HurManBlirScoutSteps